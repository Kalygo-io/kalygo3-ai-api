# Requirements for LangSmith Evaluation Scripts
# These are the core dependencies needed to run the evaluation demos

# Core LangSmith and LangChain dependencies
langsmith>=0.1.96
langchain>=0.2.12
langchain-core>=0.2.28
langchain-openai>=0.1.20

# Environment and utility
python-dotenv>=1.0.0

# For advanced features (optional)
openai>=1.44.0

# Note: Most dependencies are already in the main project requirements.txt
# This file is for reference and isolated testing
